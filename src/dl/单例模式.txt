
说明：保证一个类只有一个实例，并且提供一个访问该实例的全局访问点

优点：由于只生成一个实例对象，对系统资源开销小

常见五种实现方式：
主要：
1、饿汉式：线程安全 调用率高 不能延时加载
2、懒汉式：线程安全 调用率不高 可以延时加载
其他：
3、双重检测锁式：由于jvm底层内部原因，偶尔会出现问题 不推荐使用
4、静态内部类式：线程安全 调用率高 可以延时加载
5、枚举：线程安全 调用率高 不能延时加载(避免反射和反序列化创建对象漏洞)